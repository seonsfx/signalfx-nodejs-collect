(function() {
  var Counter,
    __bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; };

  Counter = (function() {
    function Counter() {
      this.frontend = __bind(this.frontend, this);
      this.backend = __bind(this.backend, this);
      this._count = 0;
    }

    Counter.prototype.backend = function(cb) {
      cb(this._count);
      return this._count;
    };

    Counter.prototype.frontend = function(req, res, next) {
      this._count += 1;
      if (typeof next === 'function') {
        return next();
      }
    };

    return Counter;

  })();

  module.exports = Counter;

}).call(this);
